import React from 'react';

const Loading = () => {
  return (
    <div style={{
      height: '100vh',
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      fontSize: '24px',
      fontWeight: 'bold',
    }}>
      Loading...
    </div>
  );
};

export default Loading;
